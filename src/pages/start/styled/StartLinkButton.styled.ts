import styled from 'styled-components';
import LargeButton from '../../../core/components/styled/LargeButton.styled';

const StartLinkButton = styled(LargeButton)`
  && {
    width: 300px;
    margin-top: 25px;
  }
`;

export default StartLinkButton;
