import styled from 'styled-components';
import { LargeButton } from '../../../core/components/styled/LargeButton.styled';

export const StartLinkButton = styled(LargeButton)`
  && {
    width: 300px;
    margin-top: 25px;
  }
`;
