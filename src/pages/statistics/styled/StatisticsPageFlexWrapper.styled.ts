import styled from 'styled-components';

const StatisticsPageFlexWrapper = styled.div`
  display: flex;
  height: 100%;
  margin: 0 120px;

  @media (max-width: 600px) {
    margin: 0 50px;
  }
`;

export default StatisticsPageFlexWrapper;
