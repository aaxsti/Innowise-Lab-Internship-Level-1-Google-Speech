import styled from 'styled-components';

const StatisticsPageFlexGrowWrapper = styled.div`
  flex-direction: column;
  display: flex;
  flex-grow: 1;
`;

export default StatisticsPageFlexGrowWrapper;
