import styled from 'styled-components';
import Colors from '../../../../../core/constants/colors';

const ModalWordsAmount = styled.span`
  margin-left: 5px;
  color: ${Colors.secondaryText};
`;

export default ModalWordsAmount;
